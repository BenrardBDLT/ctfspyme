<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>aHacker Login</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            background-color: #000;
            color: #fff;
            font-family: Arial, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            /* Animation de fond */
            background-color: #000;
            background-size: cover;
            background-position: center;
            position: relative;
            overflow: hidden;
        }
        .particles-js-canvas-el {
            position: absolute;
            width: 100%;
            height: 100%;
            z-index: 1;
        }
        .form-container {
            max-width: 400px;
            padding: 50px; /* Modification de la valeur de padding */
            border: 2px solid #fff;
            border-radius: 20px;
            background-color: rgba(0, 0, 0, 0.7);
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.5);
            animation: fadeIn 5s ease;
            
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        .form-control {
            background-color: #222;
            border: 1px solid #444;
            border-radius: 15px;
            color: #fff;
        }
        .form-control:focus {
            background-color: #333;
            border-color: #888;
            color: #fff;
        }
        .form-label {
            color: #fff;
            
        }
        .btn {
            border-radius: 20px;
            background-color: #dc3545;
            border: 1px solid #dc3545;
            color: #fff;
            transition: background-color 0.3s, border-color 0.3s;
        }
        .btn:hover {
            background-color: #c82333;
            border-color: #c82333;
        }
        .form-text {
            color: #999;
        }
        .d-grid{
            display: flex;
            justify-content: center;

        }
        /* Effets de survol */
        .form-control:hover,
        .btn:hover {
            transform: translateY(-2px);
            transition: transform 0.3s ease;
        }
        /* Icônes personnalisées */
        .fa-user, .fa-lock {
            position: absolute;
            top: 14px;
            left: 15px;
            color: #aaa;
        }
        .fa-lock {
            top: 52px;
        }
        /* Messages d'erreur et de succès stylisés */
        .error-message, .success-message {
            background-color: rgba(220, 53, 69, 0.8);
            color: #fff;
            border-radius: 5px;
            padding: 10px 15px;
            margin-top: 10px;
            display: none;
        }
        .success-message {
            background-color: rgba(40, 167, 69, 0.8);
        }
        /* Animations lors de l'entrée des données */
        .form-control, .btn {
            transition: all 0.3s ease;
        }
        .form-control:focus {
            transform: scale(1.05);
        }
        .btn:focus {
            transform: scale(1.05);
        }

        .photo{
            filter: brightness(65%);
            size: 30%;
            position: absolute;
            top: 50px;
            bottom: 20px;
            left: -220px;
            z-index: -1;
            animation: fadeIn 10s ease;
         }
        .giphy{
        
        }
        .popup {
        position: fixed;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        z-index: 9999;
        background-color: rgb(44, 44, 44);
        padding: 20px;
        border-radius: 5px;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.3);
        opacity: 0; /* Rendre le popup initialement transparent */
        transition: opacity 0.5s ease;
    }
    .popup.open {
        opacity: 1; 
    }
    .popup-content {
        margin-bottom: 10px;
        color: white;
    }
    
    .popup-close {
        cursor: pointer;
        color: gray;
        float: right;
    }
    .popup a {
        color: white; /* Définir la couleur du texte des liens sur blanc */
        text-decoration: none;
        font-weight: bold; /* Supprimer la décoration par défaut des liens */
    }
    
    .popup a:hover {
        color: white; /* Définir la couleur du texte des liens survolés sur blanc */
    }

    .police{
        font-size: 5px;
        margin: 0;
    }
    </style>
</head>
<body>
    <!-- Animation de fond -->
    
        

    <div class="form-container">
        <h2 class="text-center mb-4">Welcome back</h2>
        <form method="POST" action="{{ url_for ('post_login')}}">
            <div class="mb-3 position-relative">
                <label for="username" class="form-label"><i class="fas fa-user"></i> Username</label>
                <input type="text" class="form-control" id="username" name="username" aria-describedby="usernameHelp">
            </div>
            <br>
            <div class="mb-3 position-relative">
                <label for="password" class="form-label"><i class="fas fa-lock"></i> Password</label>
                <input type="password" class="form-control" id="password" name="password">
            </div>
            <br>
            <div class="d-grid">
                <button type="submit" class="btn btn-danger">Login</button>
            </div>
            

            <div class="container" onclick="showPopup(`
                <h3>Mince ... le ctf commence dès maintenant</h3>
                </br>
                
                </br><a href='/static/download/socketaide.py' download>cliquez ici ! </a>
                `)">
            <div class="'police">Help</div>
        </div>
        


            <!-- Messages d'erreur et de succès stylisés -->
            <div class="error-message">Invalid username or password</div>
            <div class="success-message">Login successful</div>
        </form>

    </div>
    
    <img src="static\img\hacker.png"  alt="Photo" class= "photo">
    <script src="static/script.js"></script>
</body>
</html>
