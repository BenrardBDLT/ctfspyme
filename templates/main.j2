<!DOCTYPE html>
<html>
<head>
  <title>Site avec Menu Dépliant</title>
  <style>
    body {
      background-color: black;
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
    }

    .container {
      display: flex;
      height: 100vh;
    }

    .menu {
        width: 0;
        background-color: #333;
        color: white;
        transition: width 0.3s;
        overflow: hidden;
        /* Ajoutez la propriété suivante */
        right: 0;
        left: auto;
        }

    .menu.open {
        width: 200px;
        /* Ajoutez la propriété suivante */
        right: auto;
        left: 0;
        }

    .content {
      flex-grow: 1;
      color: white;
      padding: 20px;
    }

    .hamburger {
      position: fixed;
      top: 20px;
      right: 20px;
      z-index: 999;
      cursor: pointer;
    }

    .hamburger span {
      display: block;
      width: 30px;
      height: 4px;
      background-color: white;
      margin-bottom: 5px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="menu" id="menu">
      <ul>
        <li><a href="#">Accueil</a></li>
        <li><a href="#">À propos</a></li>
        <li><a href="#">Services</a></li>
        <li><a href="#">Contact</a></li>
      </ul>
    </div>
    <div class="content">
      <h1>Site avec Menu Dépliant</h1>
      <p>Bienvenue sur notre site !</p>
    </div>
    <div class="hamburger" onclick="toggleMenu()">
      <span></span>
      <span></span>
      <span></span>
    </div>
  </div>

  <script>
    function toggleMenu() {
      var menu = document.getElementById("menu");
      menu.classList.toggle("open");
    }
  </script>
</body>
</html>